{
  "name": "shiftworks-platform",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --ai AI",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "playwright": "^1.58.2"
  }
}
